Host github.com
	User git
	Port 443
	Hostname ssh.github.com
	TCPKeepAlive yes
	# windows
	# ProxyCommand connect -S 127.0.0.1:7890 -a none %h %p
	# linux
	# ProxyCommand nc -x 127.0.0.1:7890 %h %p

# 格式如下:
# Host 服务器别名(自定义)
# 	User 服务器已有的用户名
# 	Port 服务器openssh开放端口(默认 22)
# 	Hostname 服务器域名(可以填IP/域名)
# 	Identityfile 私钥地址(默认 ~/.ssh/id_rsa)
